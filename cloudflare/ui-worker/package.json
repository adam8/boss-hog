{
  "name": "boss-hog-ui-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:site": "node ./scripts/generate-site-module.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "predeploy": "npm run generate:site",
    "pretest": "npm run generate:site",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.81.1"
  }
}
